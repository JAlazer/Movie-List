import { pool } from "./database";
import './dotenv.js'
import movieList from "../data/movies.js";

async function createMovieTable() {
    //query

    //try awaiting pool query

    // catch error
}

async function seedMovieTable() {
    // await the creation of the movie table

    // for each movie, use an insert query, values array, pool the insert query, values, and function to check for an error

}


seedMovieTable()